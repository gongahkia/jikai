for sg cases, add another step that attempts to scrape relevant cases from available sources in singapore jurisdiction
have further ideas for a download workflow that imports additional tort cases for training the model from scratch
resolve the failing CI/CD workflow
do a refactor of rich_app.py as necessary since its so large at the moment into various smaller files that are logically separate, then test every single function and tool to ensure no import errors and to confirm the refactor was succesful
